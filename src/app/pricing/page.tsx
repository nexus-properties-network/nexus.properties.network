import { PublicPage } from "@/components/public-page";
import { nexusPlans, pricingPolicy } from "@/config/pricing";

function formatPKR(value: number | "Custom") { return value === "Custom" ? value : `PKR ${value.toLocaleString("en-PK")}`; }

export default function PricingPage() { return <PublicPage eyebrow="Role-based plans" title={<>Choose your <em>Nexus plan.</em></>} description="Approved pricing for agents, agencies, developers, investors, corporate teams, industrial operators, listings, locks, verification, and automation services."><section className="public-section"><div className="public-plans">{nexusPlans.map((plan, index) => <article className={`public-plan ${index === 1 ? "featured-plan" : ""}`} key={plan.name}>{index === 1 && <span className="popular">Most Popular</span>}<h3>{plan.name}</h3><strong className="pkr-price">{formatPKR(plan.monthly)}<small>{plan.monthly === 0 ? " /month" : " /month"}</small></strong><p className="annual-price">Yearly: {formatPKR(plan.yearly)}</p>{plan.features.map((feature) => <p key={feature}>{feature}</p>)}<a className="button button-primary" href="/register">Choose plan <span>-&gt;</span></a></article>)}</div><div className="pricing-addons"><h2>Usage and service pricing</h2><p>Lead Lock: <strong>48 hours / PKR 500 only</strong>. Deal Lock: <strong>PKR 500–5,000</strong>. Verification: PKR 500–10,000. Listings: PKR 500, PKR 1,000, PKR 2,500, or PKR 5,000+ featured. Automation: PKR 25,000, PKR 55,000, or PKR 150,000.</p></div></section></PublicPage>; }
