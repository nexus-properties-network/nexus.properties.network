import { PublicPage } from "@/components/public-page";
import { nexusPlans, pricingPolicy } from "@/config/pricing";

export default function PricingPage() { return <PublicPage eyebrow="Role-based plans" title={<>Choose your <em>Nexus plan.</em></>} description="Each role gets the tools, permissions, AI workforce access, and workflow capacity it needs. Prices are shown in PKR and remain subject to configured billing rules."><section className="public-section"><div className="public-plans">{nexusPlans.map((plan, index) => <article className={`public-plan ${index === 2 ? "featured-plan" : ""}`} key={plan.name}>{index === 2 && <span className="popular">Most Popular</span>}<h3>{plan.name}</h3><strong>{plan.monthly === "Custom" ? plan.monthly : `PKR ${plan.monthly.toLocaleString()}`}<small>{plan.monthly === 0 ? " /free" : " /month"}</small></strong>{plan.features.map((feature) => <p key={feature}>{feature}</p>)}<p>{pricingPolicy.yearlyDiscount}</p><a className="button button-primary" href="/register">Choose plan <span>-&gt;</span></a></article>)}</div></section></PublicPage>; }
