import { WorkspaceShell } from "@/components/workspace-shell";
const agents = [["Nexus Matchmaker", "Property matching", "Running", "Matches buyer intent to verified inventory across coverage areas."], ["Nexus Verifier", "Trust & risk", "Running", "Reads documents, checks completeness, and routes risk signals for review."], ["Nexus Deal Desk", "Negotiation support", "Ready", "Summarizes offers, counter-offers, and next actions for deal teams."], ["Nexus Market Lens", "Intelligence", "Running", "Builds market briefs from activity, demand, and pricing signals."], ["Nexus Concierge", "Transaction support", "Ready", "Keeps documents, handoffs, and closing milestones moving."], ["Nexus Portfolio", "Investment analytics", "Paused", "Tracks yield, exposure, and repositioning opportunities across assets."]];
export default function AIWorkforcePage() { return <WorkspaceShell active="AI Workforce"><main className="workspace-content"><section className="page-intro"><div><span className="workspace-overline">AI operating layer</span><h2>A workforce for every decision.</h2><p>Explainable agents that accelerate property work while keeping people in control of high-stakes decisions.</p></div><button className="workspace-primary">Configure workforce</button></section><div className="ai-workforce-grid">{agents.map(([name, role, status, description], index) => <article className="ai-agent-card" key={name}><div className="ai-agent-head"><span className="ai-agent-number">0{index + 1}</span><span className={`agent-status ${status.toLowerCase()}`}><i /> {status}</span></div><h3>{name}</h3><span className="agent-role">{role}</span><p>{description}</p><div className="agent-footer"><span>Last action {index + 2} min ago</span><button aria-label={`Open ${name}`}>→</button></div></article>)}</div></main></WorkspaceShell>; }
