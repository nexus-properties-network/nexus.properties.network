import { WorkspaceShell } from "@/components/workspace-shell";

const queue = [["Plot 771, Bahria Town", "Islamabad", "3 of 4 checks complete", "Medium risk"], ["Apartment 08, Askari 14", "Rawalpindi", "2 of 4 checks complete", "Documents missing"], ["House 214, DHA Phase 6", "Lahore", "4 of 4 checks complete", "Ready to approve"]];
export default function VerificationPage() { return <WorkspaceShell active="Verification"><main className="workspace-content"><section className="page-intro"><div><span className="workspace-overline">Trust infrastructure</span><h2>Verification queue.</h2><p>Make every property and participant easier to trust with auditable review signals.</p></div><button className="workspace-primary">Review next item</button></section><section className="verification-metrics"><div><strong>86.4%</strong><span>Network verification coverage</span></div><div><strong>18</strong><span>Items awaiting review</span></div><div><strong>4.2 hrs</strong><span>Average review time</span></div></section><section className="workspace-panel table-panel"><div className="table-heading"><span>Asset</span><span>Progress</span><span>Risk signal</span><span>Reviewer</span><span /></div>{queue.map(([name, city, progress, risk]) => <div className="property-row" key={name}><div className="verification-icon">✓</div><div><strong>{name}</strong><p>{city} · {progress}</p></div><span className="progress-line"><i style={{ width: progress.startsWith("4") ? "100%" : progress.startsWith("3") ? "75%" : "50%" }} /></span><span className={`status-label ${risk === "Ready to approve" ? "verified" : "review"}`}>{risk}</span><span className="row-muted">Nexus review</span><button className="row-menu">→</button></div>)}</section></main></WorkspaceShell>; }
