import Link from "next/link";

const workspaceNav = [["Overview", "/dashboards"], ["Properties", "/properties"], ["Leads", "/lead-protection"], ["Verification", "/verification"], ["AI Workforce", "/ai-workforce"], ["Investments", "/investments"]] as const;

export function WorkspaceShell({ children, active }: Readonly<{ children: React.ReactNode; active: string }>) {
  return <div className="workspace-shell"><aside className="workspace-sidebar"><Link className="workspace-brand" href="/"><img src="/nexus-assets/transparentlogo.png" alt="Nexus Property Network" /></Link><div className="workspace-context"><span className="workspace-context-label">Workspace</span><strong>Pakistan Network</strong><span>Operator view</span></div><nav className="workspace-nav" aria-label="Workspace navigation">{workspaceNav.map(([label, href]) => <Link className={active === label ? "active" : ""} href={href} key={label}><span className="nav-glyph" aria-hidden="true">{label.slice(0, 1)}</span>{label}</Link>)}</nav><div className="workspace-sidebar-footer"><Link href="/">View public site</Link><span className="trust-status"><i /> Systems operational</span></div></aside><div className="workspace-main"><header className="workspace-header"><div><span className="workspace-overline">Monday, 21 September 2026</span><h1>{active}</h1></div><div className="workspace-actions"><button className="icon-button" aria-label="Notifications">3</button><button className="profile-chip"><span className="avatar">AK</span><span><strong>Ahmed Khan</strong><small>Platform admin</small></span><b>⌄</b></button></div></header>{children}</div></div>;
}
